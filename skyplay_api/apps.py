from django.apps import AppConfig

class SkyplayAPIConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'skyplay_api'
    verbose_name = 'Skyplay API'  # Change the display name in Django Admin
